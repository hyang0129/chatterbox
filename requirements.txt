# PyTorch — cu128 build required for Blackwell (sm_120, RTX 50xx) support.
# Install before chatterbox-tts so its torch dependency resolves to this version.
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.7.0
torchaudio>=2.7.0

# Core TTS
chatterbox-tts>=0.1.6

# Server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Dev
httpx>=0.27.0
python-dotenv>=1.0.0
pytest>=8.0.0
ruff>=0.9.0
